#ifndef GENERATE_WORKLOAD_H
#define GENERATE_WORKLOAD_H

int generateRandomWorkload();
int * generateRandomWorkloadArray(int numOfElements);
void printWorkloadArray(int * workload, int numOfElements);

#endif